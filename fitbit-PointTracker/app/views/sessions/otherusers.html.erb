  <div class="container side-collapse-container">
    <div class="row name text-center">
      <h1>Add Friends</h1>
      <div class="input-wrap"><input type="text" placeholder="Jimmy" /></div>
    </div>
     <div class="row text-center custyle points">
            <table class="table table-striped custab">
            <thead>
            <div class="text-center"><h3>Users</h3></div>
                <tr style="background: #474747; color: #fafafa;">
                    <th>Name</th>
                    <th>Points</th>
                </tr>
            </thead>
            <% @users.each do |user| %>
                    <tr>
                        <td><%= user.name %></td>
                        <td><%= user.points %></td>
                    </tr>
            <% end %>
            </table>
     </div>
 </div>